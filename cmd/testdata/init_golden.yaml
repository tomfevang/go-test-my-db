# Generated by: go-seed-my-db init
# Review and uncomment sections as needed.

options:
  dsn: "testuser:***@tcp(localhost:3306)/testdb"
  rows: 1000
  batch_size: 1000
  workers: 4

tables:
  users:
    # rows: 1000
    # columns:
    #   email: '{{ Email() }}'  # detected heuristic
    #   first_name: '{{ FirstName() }}'  # detected heuristic
    #   last_name: '{{ LastName() }}'  # detected heuristic
    #   created_at: '{{ DateRange(2020-2025) }}'  # detected heuristic

  orders:
    # rows: 1000
    # references:
    #   user_id: users.id
    # columns:
    #   status:  # enum: [pending, shipped, delivered]
    #   total: '{{ Price(1, 1000) }}'  # detected heuristic

  products:
    # rows: 1000
    # columns:
    #   name: '{{ Name() }}'  # detected heuristic
    #   price: '{{ Price(1, 1000) }}'  # detected heuristic

# tests:
#   - name: example query
#     query: SELECT COUNT(*) FROM ...
#     repeat: 5
